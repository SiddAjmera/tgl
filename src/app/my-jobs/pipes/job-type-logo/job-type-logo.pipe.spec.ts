import { JobTypeLogoPipe } from './job-type-logo.pipe';

describe('JobTypeLogoPipe', () => {
  it('create an instance', () => {
    const pipe = new JobTypeLogoPipe();
    expect(pipe).toBeTruthy();
  });
});
